        <footer class="site-footer">
			<div class="container">
				<div class="row justify-content-center align-items-center">
					<div class="col-12 col-md-auto">
						<img class="footer-logo" src="{{ TimberImage(footer_logo).src }}" alt="P3" />
					</div>
					<div class="col-12 col-md-auto col-apps">
						{% for app in apps %}
							<div class="app" id="app-{{ loop.i }}">
								<img class="app-icon" src="{{ TimberImage(app.icon).src }}" alt="{{ app.name }}" data-toggle-class="-open" data-toggle-target="#app-{{ loop.i }}" />
								<h6 class="app-name">{{ app.name }}</h6>
							</div>
						{% endfor %}
					</div>
					<div class="col-12 col-md-auto">
						<span class="feature-list-accent"></span>
						<ul class="feature-list">
							{% for feature in features %}
								<li class="feature">{{ feature.feature }}</li>
							{% endfor %}
						</ul>
					</div>
				</div>
			</div>
        </footer>
        {{ wp_footer }}
    </body>
</html>